server:
  port: 8888
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springshiro?characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
  jpa:
    hibernate:
      ddl-auto: none
